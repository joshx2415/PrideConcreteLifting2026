## 2024-05-23 - Contact Form Loading State
**Learning:** Adding a loading state to the contact form significantly improves the user experience by providing immediate feedback during submission. The use of `aria-busy` and disabling the button ensures accessibility.
**Action:** When implementing forms in Vue components, always include a reactive `isSubmitting` state to manage the UI during async operations.
